# Test adapter

Small LoRA adapter made by `scripts/smoke_tiny.py` with the bundled test model.

Not a real model. Not for general use. It exists only to show that training, saving, and reloading work.

Base model: test stand-in, not Stable Audio Open. License for this test output: same as the Audiyo code (Apache-2.0).
