# Benchmark modules

* `benchutils/env.py` - library versions and hardware header.
* `benchutils/runner.py` - latency helpers.
* `benchmark.py` - warmup plus repeats, timing, memory snapshots.
* `benchmarks/` - configs and per mode runner that writes JSON.
